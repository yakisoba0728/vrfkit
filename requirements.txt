# Python dependencies for tools/.
#
# The Rust workspace needs nothing from this file; it exists for the scripts in
# tools/ and for CI's python-checks job.
#
# Derived by walking every tools/*.py with ast and subtracting the stdlib and
# this repo's own modules -- not by grepping for imports at column 0, which is
# how numpy was missed on the first attempt (to_valplay_bundle.py imports it
# inside a function).
#
# Neither is optional. Without pyarrow eleven test modules fail to import;
# without numpy, four more. CI ran for months executing 288 of 376 tools tests
# because nothing installed either one.
pyarrow>=15
numpy>=1.26
